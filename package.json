{
  "name": "keyjump",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension: press ; to label every link, button and field, then type to jump.",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "icons": "node scripts/make-icons.mjs",
    "check": "node scripts/check.mjs",
    "build": "pnpm icons && pnpm check",
    "zip": "pnpm build && mkdir -p dist && rm -f dist/keyjump.zip && zip -qr dist/keyjump.zip manifest.json icons src -x '*.DS_Store' && echo 'packed dist/keyjump.zip'",
    "demo": "node scripts/serve.mjs"
  }
}
